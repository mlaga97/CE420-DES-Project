#pragma once

#include <bitset>
#include <string>
#include <iostream>

void dumpBits28(std::string name, uint64_t input);
void dumpBits32(std::string name, uint64_t input);
void dumpBits48(std::string name, uint64_t input);
void dumpBits56(std::string name, uint64_t input);
void dumpBits64(std::string name, uint64_t input);
